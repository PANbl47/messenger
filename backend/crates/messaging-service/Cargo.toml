[package]
name = "messaging-service"
version = "0.1.0"
edition.workspace = true

[dependencies]
sync-engine = { path = "../../../core/rust-core/crates/sync-engine" }
tokio.workspace = true

[dev-dependencies]
tokio.workspace = true
